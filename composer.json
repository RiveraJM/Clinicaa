{
  "name": "clinica/proyecto",
  "description": "Sistema backend PHP",
  "type": "project",
  "require": {
    "php": ">=8.0"
  },
  "autoload": {
    "classmap": [
      "controllers/",
      "config/",
      "includes/",
      "api/",
      "cron/"
    ]
  },
  "scripts": {
    "test": "phpunit --colors=always --testdox"
  }
}
